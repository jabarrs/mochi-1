// import axios from "axios";

// Mengatur axios di window untuk akses global
// window.axios = axios;

// Menetapkan konfigurasi default untuk axios
// axios.defaults.withCredentials = false;

// Menggunakan HTTPS dan menggabungkan hostname dengan URL backend
// let backendUrl = `https://be-rest.vercel.app`;

// Atur baseURL axios
// axios.defaults.baseURL = backendUrl;

// console.log('Base URL:', axios.defaults.baseURL);
